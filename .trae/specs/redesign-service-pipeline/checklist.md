- [x] 新领域模型已落地：仓库、需求分支、服务阶段、PR 历史、事件审计、服务主视角快照均有明确表结构
- [x] 服务只能关联与需求分支同仓库的服务
- [x] 每个服务可定义独立阶段流水线，并支持启停、排序与目标分支配置
- [x] 系统可保存同一阶段的多次 PR 记录，而不是只保留最新 PR URL
- [x] 系统可记录创建 PR、合并、关闭、撤销合并和同步行为的历史事件
- [x] 任务负责人和分支开发者以不同关联关系保存
- [x] 服务主视角页 `services/[id]` 已存在
- [x] 服务主视角页支持阶段 `Tab`
- [x] 打开某阶段 `Tab` 时可看到“未合并需求分支”和“已合并需求分支”
- [x] 服务主视角页支持阶段横向矩阵或等价切换视图
- [x] 服务主视角可展示当前阶段状态、PR 状态、最近同步时间和可操作状态
- [x] GitHub 集成改为使用显式仓库标识而不是服务名推导 repo
- [x] 状态同步后会刷新服务主视角快照
- [x] `pnpm lint` 通过
- [x] `pnpm typecheck` 通过
- [x] 修改需求分支仓库归属时，系统仍能保证“需求分支只能关联同仓库服务”的约束
- [x] 任务、服务页和仪表盘的服务统计以稳定的服务标识聚合，而不是依赖服务名称字符串
- [x] 任务负责人与分支开发者已通过 `users` / `task_assignments` / `task_branch_developers` 形成完整 API 与页面闭环
- [x] `/repositories` 页面已存在，并可查看仓库、SCM 绑定及仓库下的服务/需求分支概览
- [x] 代码库内的开发文档已移除旧 `service_branches` / `github_configs` / `testBranch/masterBranch` 设计描述
